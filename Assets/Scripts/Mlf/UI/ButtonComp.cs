using System;
using UnityEngine;
using UnityEngine.UI;

namespace Mlf.UI
{
    public class ButtonComp : MonoBehaviour
    {
        
        private void Start()
        {
            //MouseInputManagerSystem.instance.onCanBuildChanged += onActiveChanged;
            //find imaage object
           
        }

       

       

        /*
        public void build()
        {
            Debug.Log("Build Started");
            /*
            GridSystem.AddBuildingToGrid(
                MouseInputManagerSystem.instance.placeBuildingSO.id,
                in MouseInputManagerSystem.instance.placeBuildingGridPos,
                in MouseInputManagerSystem.instance.placeBuildingSO.size,
                GridSystem.currentMapData.id);


            Debug.Log("Adding modification to gridSystem");
            BuildingManager.Instance.AddBuilding(
                MouseInputManagerSystem.instance.gridBuildingItem,
                in GridSystem.currentMapData);
          



            //MouseInputManagerSystem.instance.stopBuildSetup();

         }
        */
    }

}
